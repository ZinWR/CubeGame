import { Plugin } from 'vite';
import { FilterPattern } from '@rollup/pluginutils';

interface Options {
    include?: FilterPattern;
    exclude?: FilterPattern;
    compress?: boolean | ((code: string) => string | Promise<string>);
}
declare function export_default(userOptions?: Options): Plugin;
declare function defaultCompress(code: string): string;

export { type Options, export_default as default, defaultCompress };
